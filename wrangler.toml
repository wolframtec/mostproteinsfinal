name = "mostproteinsfinal"
compatibility_date = "2024-01-01"

# Cloudflare Pages build configuration
# Build command must be set in dashboard: cd app && npm install && npm run build
pages_build_output_dir = "app/dist"
